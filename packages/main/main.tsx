import ReactDOM from 'react-dom/client';
import { AppShell } from '@my/app-shell';

const rootElement = document.getElementById('root')!;
const root = ReactDOM.createRoot(rootElement);
root.render(<AppShell />);
